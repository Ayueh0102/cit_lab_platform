# ========================================
# 校友平台生產環境配置
# 使用方式：複製此檔案為 .env，填入實際值
# ========================================

# ========== 資料庫配置 ==========
# PostgreSQL (生產環境推薦)
DATABASE_URL=postgresql://alumni:CHANGE_ME_PASSWORD@db:5432/alumni_platform

# 或 SQLite (簡單部署)
# DATABASE_URL=sqlite:///./app_v2.db

# ========== Flask 配置 ==========
FLASK_ENV=production
FLASK_DEBUG=False
SECRET_KEY=change-me-to-a-secure-random-string-at-least-32-chars

# ========== JWT 認證 ==========
JWT_SECRET_KEY=change-me-to-another-secure-random-string-at-least-32-chars
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# ========== CORS 設定 ==========
# 允許的來源（多個用逗號分隔）
ALLOWED_ORIGINS=yourdomain.com,www.yourdomain.com,api.yourdomain.com

# ========== SMTP 郵件設定 ==========
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_SENDER_EMAIL=noreply@yourdomain.com
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password-not-account-password
SMTP_USE_TLS=True

# ========== 前端環境變數 ==========
# API 端點 (如前端部署在不同主機)
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# ========== 日誌配置 ==========
LOG_LEVEL=INFO
LOG_FILE=/var/log/alumni-platform/app.log

# ========== Redis (可選，用於 session/cache) ==========
# REDIS_URL=redis://redis:6379/0

# ========== AWS S3 (可選，用於檔案存儲) ==========
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_S3_BUCKET=alumni-platform-uploads
# AWS_S3_REGION=ap-southeast-1

# ========== 其他功能 ==========
# 維護模式
MAINTENANCE_MODE=False

# API 速率限制
RATE_LIMIT_ENABLED=True
RATE_LIMIT_PER_MINUTE=60

# 檔案上傳限制 (MB)
MAX_UPLOAD_SIZE_MB=20
