# 🚀 快速啟動指南

這份指南將協助你快速設置並執行色彩與照明科技研究所系友會社群平台。

## 📋 前置需求

請確保你的系統已安裝以下工具：

- **Node.js** (建議 v18 或以上)
- **Python 3.9+**
- **npm** 或 **pnpm**

## ⚙️ 一、環境設置

### 1. 後端 Flask API 設置

```bash
# 進入後端目錄
cd alumni_platform_api

# 建立 Python 虛擬環境
python3 -m venv venv

# 啟動虛擬環境
source venv/bin/activate  # macOS/Linux
# 或
venv\Scripts\activate     # Windows

# 升級 pip
pip install --upgrade pip

# 安裝依賴套件
pip install Flask flask-cors Flask-SQLAlchemy PyJWT
```

### 2. 前端 React 應用設置

```bash
# 進入前端目錄 (推薦使用原始版本)
cd alumni-platform

# 安裝 npm 依賴 (使用 legacy-peer-deps 解決相依性衝突)
npm install --legacy-peer-deps
```

## 🎯 二、啟動應用程式

### 啟動後端伺服器

```bash
# 在 alumni_platform_api 目錄下
source venv/bin/activate
python src/main.py
```

後端伺服器將在 **http://localhost:5001** 啟動

✅ 成功啟動時會顯示：
```
 * Running on http://127.0.0.1:5001
 * Running on http://10.x.x.x:5001
```

**注意**: 首次啟動時會自動建立資料庫並載入測試資料，包含：
- 3 個測試使用者（管理員、王小明、李美華）
- 3 個職缺範例
- 2 個活動範例
- 3 個公告範例

### 啟動前端開發伺服器

開啟**新的終端視窗**，然後執行：

```bash
# 在 alumni-platform 目錄下
npm run dev
```

前端應用將在 **http://localhost:5173** 啟動

## 🔐 測試帳號

系統已預先建立以下測試帳號供使用：

| 電子郵件 | 密碼 | 角色 | 說明 |
|---------|------|------|------|
| admin@example.com | admin123 | 管理員 | 可存取管理後台 |
| wang@example.com | password123 | 一般用戶 | ASUS 光學工程師 |
| lee@example.com | password123 | 一般用戶 | MediaTek 色彩科學研究員 |

## 🎨 訪問應用程式

1. 開啟瀏覽器前往 **http://localhost:5173**
2. 使用上方測試帳號登入
3. 開始探索系友會平台功能！

## 📱 主要功能

- ✅ **系友名錄** - 瀏覽系友資料、搜尋聯繫
- ✅ **職缺分享** - 發布與瀏覽職缺、請求交流
- ✅ **活動管理** - 檢視活動列表、線上報名
- ✅ **公佈欄** - 查看系友會公告與系友動態
- ✅ **個人檔案** - 編輯個人資料、工作經歷
- ✅ **通知中心** - 接收系統通知
- ✅ **管理後台** - 系統統計（僅管理員）

## 🛠️ 開發指令參考

### 前端指令

```bash
npm run dev      # 啟動開發伺服器
npm run build    # 建置生產版本
npm run preview  # 預覽生產版本
npm run lint     # ESLint 程式碼檢查
```

### 後端指令

```bash
python src/main.py              # 啟動開發伺服器
# 資料庫會在 src/database/app.db 自動建立
```

## 🔧 常見問題排解

### 問題 1: npm install 失敗

**解決方案**: 使用 `--legacy-peer-deps` 標記
```bash
npm install --legacy-peer-deps
```

### 問題 2: Python 虛擬環境啟動失敗

**解決方案**: 確保使用正確的啟動指令
```bash
# macOS/Linux
source venv/bin/activate

# Windows
venv\Scripts\activate
```

### 問題 3: 後端 port 5001 被佔用

**解決方案**: 編輯 `alumni_platform_api/src/main.py` 最後一行，更改 port 號碼：
```python
app.run(host='0.0.0.0', port=5002, debug=True)  # 改成其他 port
```

同時需要更新前端 API 呼叫的 URL。

### 問題 4: 資料庫錯誤

**解決方案**: 刪除舊資料庫並重新啟動
```bash
cd alumni_platform_api
rm -rf src/database/app.db
python src/main.py  # 會自動重建資料庫
```

## 📂 專案結構說明

```
alumni-platform-complete-final/
├── alumni-platform/           # 前端 React 應用 (推薦)
│   ├── src/
│   │   ├── App.jsx           # 主應用程式
│   │   └── components/       # UI 元件
│   └── package.json
├── alumni_platform_api/       # 後端 Flask API
│   ├── src/
│   │   ├── main.py           # API 入口點
│   │   ├── models/           # 資料模型
│   │   └── routes/           # API 路由
│   └── requirements.txt
└── QUICKSTART.md             # 本檔案
```

## 🎯 下一步

- 閱讀 `CLAUDE.md` 了解詳細的開發指南
- 查看 `alumni_platform_specification.md` 了解完整功能規格
- 參考 `api_specification.md` 了解 API 端點細節
- 檢視 `ui_ux_design.md` 了解設計理念

## 💡 開發技巧

1. **後端修改即時生效**: Flask 開發模式會自動重載
2. **前端熱更新**: Vite 提供即時的 HMR (Hot Module Replacement)
3. **API 測試**: 可使用 Postman 或 curl 測試 API 端點
4. **資料庫檢視**: 使用 SQLite Browser 檢視 `src/database/app.db`

---

**有問題嗎？** 查看完整文件或修改程式碼開始你的開發之旅！ 🚀