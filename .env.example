# ==================== 開發環境 ====================
# Docker Compose 開發環境變數範本
# 複製到 .env 並根據需要修改

# 前端
NEXT_PUBLIC_API_URL=http://localhost:5001

# 後端 - Flask
SECRET_KEY=dev-secret-key-change-in-production
JWT_SECRET_KEY=dev-jwt-secret-change-in-production
DATABASE_URL=sqlite:///src/database/app_v2.db
FLASK_ENV=development
ALLOWED_ORIGINS=http://localhost:3000,http://frontend:3000

# ==================== 生產環境 ====================
# 使用 docker-compose.prod.yml 時的環境變數
# 所有敏感資訊必須從環境變數讀取，不能寫入檔案

# 資料庫 (PostgreSQL)
POSTGRES_USER=alumni
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_DB=alumni_platform

# 後端安全金鑰 (生成方式: python -c "import secrets; print(secrets.token_hex(32))")
# SECRET_KEY=<生成的 32 位元十六進制字串>
# JWT_SECRET_KEY=<生成的 32 位元十六進制字串>

# 網域設定
ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com
NEXT_PUBLIC_API_URL=https://your-domain.com/api

# SMTP (可選，用於郵件功能)
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Redis (可選，用於快取和 session 儲存)
# REDIS_URL=redis://localhost:6379/0

# AWS S3 (可選，用於檔案儲存)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_S3_BUCKET=your-bucket-name
# AWS_S3_REGION=us-east-1
